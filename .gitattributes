# LF 강제 — Windows(core.autocrlf=true)에서도 .sh/.md 가 CRLF로 변환되지 않게.
# 스크립트는 LF 의무(CRLF면 shebang·`^---$` frontmatter 파싱이 깨진다).
* text=auto eol=lf
*.sh   text eol=lf
*.md   text eol=lf
*.txt  text eol=lf
*.json text eol=lf
